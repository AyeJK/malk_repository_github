// Dynamic metadata for profile pages is handled in '[id]/page-metadata.ts'.

export default function ProfileLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <>{children}</>
  )
}
