export const metadata = {
  title: 'Login - Malk - Social Video Discovery',
  description: 'Log in to your Malk account to discover, share, and connect through social video.',
};

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <>{children}</>
  )
}
