export const metadata = {
  title: 'Malk - Social Video Discovery',
  description: 'Discover, share, and connect through social video on Malk.',
};

export default function LandingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
} 